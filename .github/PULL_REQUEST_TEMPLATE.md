## Description

<!--
What this PR does and why (intent and trade-offs, not a line-by-line log).
If it maps to an ADR, link it, e.g.:
Per [ADR 0007](https://github.com/LiangYingC/drone-go-hub/blob/main/docs/adr/0007-tech-stack-zustand-state.md), adopt Zustand…
If an architecture/flow diagram would make the design clearer, include one
(GitHub renders ```mermaid``` code blocks).
-->

## Review Tips by Commits

<!--
Guide the reviewer commit-by-commit; link every commit so each can be reviewed on its own.
Format (use the full commit URL so the subject becomes the link text;
a bare SHA also auto-links on GitHub):
- [**`<commit subject>`**](https://github.com/LiangYingC/drone-go-hub/commit/<full-sha>) — which files to look at / what to confirm.
-->

-

## Scope

- **In**: what this PR covers.
- **Out**: deliberately deferred to a later PR.

## Test

<!--
How this was verified, and how a reviewer can re-verify. List the commands run
and what they confirm, e.g.:
- `pnpm build` / `pnpm test` / `pnpm typecheck` / `pnpm lint` — all green
- manual: `pnpm dev`, then <steps → expected result>
-->

-
